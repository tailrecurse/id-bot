{
  "name": "id-bot",
  "type": "module",
  "private": true,
  "scripts": {
    "vercel": "vercel",
    "setup": "if [ -f .env ]; then tsx --env-file=.env scripts/setup.ts; else tsx scripts/setup.ts; fi",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^25.3.0",
    "prettier": "^3.8.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3",
    "vercel": "^50.22.1"
  }
}
